@{
  Layout = "_Layout";
}

@using Library.Models;

<h2>Author: @Model["author"].GetFirstName() @Model["author"].GetLastName()</h2>
<hr />
@if (@Model["authorBooks"].Count != 0)
{
  <div class="well">
    <h4>This author has these books:</h4>
    <ul>
      @foreach (Book book in @Model["authorBooks"])
      {
        <li>@book.GetTitle()</li>
      }
    </ul>
  </div>
}
<div class="well">
<h4>Add a book to this author:</h4>
<form class="form-inline" action="/authors/@Model["author"].GetId()/authors/new" method="post">
  <div class="form-group">
    <label for="book-id">Select a book</label>
    <select id="book-id" name="book-id" type="text">
      @foreach (var book in @Model["allBooks"])
      {
        <option value="@book.GetId()">@book.GetTitle()</option>
      }
    </select>
  </div>
  <button type="submit" class="btn btn-default">Add</button>
</form>
</div>
